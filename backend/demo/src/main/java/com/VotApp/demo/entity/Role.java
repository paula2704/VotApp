package com.VotApp.demo.entity;

// Enum que define los dos únicos roles posibles en el sistema
// ADMIN: puede crear, editar y eliminar encuestas
// USER: solo puede ver encuestas activas y votar
public enum Role {
    ADMIN, USER
}